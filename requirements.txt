# ─────────────────────────────────────────────────────────────────────────────
# RAG Comparison Experiment — Python Dependencies
# Install with: pip install -r requirements.txt
# Python 3.9+ compatible
# ─────────────────────────────────────────────────────────────────────────────

# ── LLM Providers ────────────────────────────────────────────────────────────
anthropic>=0.40.0          # Anthropic Claude API client
openai>=1.50.0             # OpenAI API client

# ── Embeddings ───────────────────────────────────────────────────────────────
sentence-transformers>=2.2.0   # Local embedding model (all-MiniLM-L6-v2)

# ── Vector Store ─────────────────────────────────────────────────────────────
chromadb>=0.4.0            # Persistent local vector database

# ── ArXiv Data Fetching ──────────────────────────────────────────────────────
arxiv>=2.1.0               # ArXiv Python API for fetching paper metadata
PyMuPDF>=1.23.0,<1.25.0   # PDF text extraction — 1.23/1.24 support Python 3.9

# ── Knowledge Graph (GraphRAG) ───────────────────────────────────────────────
networkx>=2.8              # Graph data structure — 2.8+ works on Python 3.9

# ── Text Processing ──────────────────────────────────────────────────────────
langchain>=0.3.0           # Text splitting utilities + LangChain wrappers
langchain-anthropic>=0.3.0 # LangChain wrapper for Anthropic (used by RAGAS)
langchain-openai>=0.2.0    # LangChain wrapper for OpenAI (used by RAGAS)
langchain-community>=0.3.0 # Community integrations including HuggingFace embeddings

# ── Evaluation ───────────────────────────────────────────────────────────────
ragas>=0.2.0               # RAG evaluation framework (faithfulness, relevancy, precision)
datasets>=2.14.0           # HuggingFace datasets (required by RAGAS)

# ── Data & Analysis ──────────────────────────────────────────────────────────
pandas>=1.5.0              # Results dataframe
numpy>=1.24.0              # Numerical operations

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.6.0          # Bar charts and heatmaps
streamlit>=1.30.0          # Web dashboard (app.py)
plotly>=5.15.0             # Interactive radar chart
kaleido>=0.2.1             # Plotly PNG export
seaborn>=0.12.0            # Better heatmap styling

# ── Utilities ────────────────────────────────────────────────────────────────
tenacity>=8.2.0            # Retry logic with exponential backoff
tqdm>=4.64.0               # Progress bars for long loops
python-dotenv>=0.21.0      # Load .env files for API keys
tabulate>=0.9.0            # Pretty-print summary tables in terminal
